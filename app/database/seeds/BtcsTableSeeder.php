<?php

class BtcsTableSeeder extends Seeder {

	public function run()
	{
		// Uncomment the below to wipe the table clean before populating
		// DB::table('btcs')->truncate();

		$btcs = array(

		);

		// Uncomment the below to run the seeder
		// DB::table('btcs')->insert($btcs);
	}

}
